# QOS API (server-only — never expose secret keys in NEXT_PUBLIC_*)
QOS_API_BASE_URL=https://ca-qos-dev-api.gentleplant-cc8574e8.uaenorth.azurecontainerapps.io
# Optional localhost-only fallback when host resolution is unavailable during local dev.
QOS_STOREFRONT_PUBLIC_ID=stf_flowers-dev-storefront_ac466b3f
QOS_LOCATION_PUBLIC_ID=loc_flowers_main
# Server-only demo product for sandbox checkout handoff
QOS_TEST_PRODUCT_PUBLIC_ID=
# Comma-separated OAuth providers shown on sign-in (requires matching QOS API credentials)
QOS_CUSTOMER_OAUTH_PROVIDERS=google,microsoft
